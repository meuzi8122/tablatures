export default async function BookmarkPage() {
    return (
        <div className="container mx-auto">
            <div className="h-screen w-screen flex items-center justify-center">
                <p>ブックマークしたTAB譜は今後閲覧可能になります。</p>
            </div>
        </div>
    );
}
